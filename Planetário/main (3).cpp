#include "Cabeçalho.h"
inline void keep_windows_open() { char ch; cin >> ch; }
using namespace std;

int main() {
	/*string text = "Teste.txt";
	Integrator teste(text);
	teste.build.init_builder();
	teste.print_point();*/
	vector<double> aa;
	for (int i = 0; i < 3; ++i) {
		aa.push_back(0);
	};

	int a;
	cin >> a;
};